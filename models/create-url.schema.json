{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "create-url",
  "description": "Create URL request model (restricted)",
  "type": "object",
  "required": [ "url" ],
  "properties": {
    "url": {
      "type": "string",
      "description": "The URL to shorten"
    },
    "custom_path": {
      "type": "string",
      "description": "A custom path for the short URL generated"
    }
  }
}
